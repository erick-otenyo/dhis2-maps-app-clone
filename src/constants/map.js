// string to use for naming mapboxstyle data sources. Also used for reverse layerId extraction when map is clicked. Should be unique so as not to possibly appear in any layer's id or name.
export const SOURCE_KEY = "$source$";
